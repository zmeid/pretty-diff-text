name: example
description: Demo of PrettyDiffText
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pretty_diff_text:
    path: ../